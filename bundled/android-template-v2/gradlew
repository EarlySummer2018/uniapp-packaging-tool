#!/usr/bin/env sh
set -e

if command -v gradle >/dev/null 2>&1; then
  exec gradle "$@"
fi

echo "Gradle wrapper jar is not bundled in this template and system gradle was not found." >&2
echo "Install Gradle or replace this script with a standard Gradle wrapper generated by 'gradle wrapper'." >&2
exit 127
